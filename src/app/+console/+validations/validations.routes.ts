import { Routes } from '@angular/router';
import { ValidationsContainerComponent } from './components';

export const routes: Routes = [
  {
    path: '',
    component: ValidationsContainerComponent,
  },
];
